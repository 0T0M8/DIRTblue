# DIRTblue
DIRTblue is a networked database server using the language of the Gods(C). It uses the browser as a client with a sleek GUI for easy interactions with the database(querying). Tge database responds to these queries with data.
